# Changelog

## v3 게이트 — 2026-09-02

사람 글도 늘 "고칠 목록 있음"이 나오던 게이트를 세 층으로 나눴다. 게이트가 매번 울면
무시되거나 멀쩡한 문장을 고치게 된다.

- **Windows 한글 콘솔에서 죽던 문제 수정.** 첫 출력 줄의 `—` 기호에서 `UnicodeEncodeError`로
  멈췄다. stdout을 UTF-8로 고정. 안내문의 `python3`는 `python`으로(Windows의 `python3`는
  스토어 안내 껍데기라 실행되지 않는다).
- **하드 게이트(exit 2)에 숫자·직접 인용 보존 추가.** SKILL 2단계가 적어 두라던 protected
  span을 이제 코드가 원본과 대조한다 — 각주와 같은 방식(등장 횟수까지). 숫자가 바뀌거나
  따옴표 안 문장이 달라지면 변환본 채택 금지.
- **exit 1은 사용자가 확정한 금지 문형만.** 대시, '아니라', 문두 경성 접속부사, 강조어·가짜
  강조·메타 선언, 450자+ 문단, 지시어 5개+ 문단, 선언 연쇄, 영문 용어 유실, 원본에 없던 각주.
- **참고로 내린 것(exit 무관):** 삼항 후보, 쉼표 지도, 미들닷, 블록리스트의 나머지 섹션.
  "김치 남았냐고, 남았다고, 그게 다였다"가 삼항으로, "책상 위에서"가 금지어로 걸리던 것.
- **선언 연쇄는 재선언일 때만.** 짧은 문장 3연속이 아니라, 인접 문장이 같은 내용어를
  되풀이할 때("사람이다. 사람이 곧 답이다")만 잡는다.
- **블록리스트 층은 `phrases.md` 섹션 제목의 태그로.** `[게이트]`(7·8·9절)는 고칠 목록,
  `[설명]`(3·6·11·12절)은 코드가 읽지 않음, 태그 없음은 참고. 설명문이 쉼표에서 잘려
  "위에서", "문자 그대로의", "코칭으로 전환" 같은 엉뚱한 금지어가 되던 사고를 없앴다.
- **테스트 8건과 CI 추가**(`tests/test_gate.py`, Ubuntu·Windows). 사람 글 exit 0, AI 글
  exit 1, 각주·숫자·인용 유실 exit 2, cp949 콘솔 생존.
- README를 v2 이후 코드에 맞게 다시 썼다(설치 경로의 `<you>` 자리표시자, "8단계"→7단계).
- SKILL.md: "주제어 보호"와 "금지 문형 우선"의 충돌을 정리 — 보호하는 것은 어휘, 금지하는
  것은 문형.

## v2 게이트 — 2026-08-19

- 각주를 set이 아니라 Counter로 대조(같은 마커가 줄어도 exit 2).
- 부호(— – ·)를 코드가 직접 센다. '아니라' 정정 골격, 문두 접속부사, 문단 지도, 지시어
  밀도, `--protect`.

## v1 — 2026-08-19

- 첫 공개. SKILL.md · phrases.md · mary_gate.py.
