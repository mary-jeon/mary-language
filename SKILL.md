---
name: mary-language
description: >
  이미 작성된 글을 '사람 작가가 쓴 글'로 변환하는 한국어 윤문 스킬. 새로 쓰는 게
  아니라 입력된 완성 글의 AI 티를 걷어내 사람 문체로 바꾼다. "사람답게",
  "작가 말투로", "AI 티 빼줘", "윤문", "이 글 변환해줘" 요청에 발동한다.
---

# mary-language

**이 스킬은 변환기다.** 새 글을 쓰는 도구가 아니다.
- **입력**: 이미 완성된 글 (초고·본문·원고).
- **출력**: 같은 내용·같은 구성·같은 사실. 문체만 사람 쪽으로.
- 내용을 더하거나 빼지 않는다. 문단을 새로 지어내지 않는다. **원문을 변환할 뿐이다.**

변환의 방법은 "매끄럽게 다듬기"가 아니다. 매끄럽게만 만들면 같은 조립이 된다.
원문에서 고른 **가장 사람다운 문단의 목소리로, 걸리는 문단을 다시 쓴다.**

## 왜 금지 리스트만으로 안 되나

금지는 회피를 낳는다. em대시를 막으면 쉼표로, "아니라"를 막으면 "~기보다"로 같은
짓을 한다. 그래서 이 스킬은 **① 생성은 목소리로 몰고(리스트 안 봄) ② 리스트는 다 쓴
뒤 탐지 힌트로만** 쓴다. 블록리스트는 [references/phrases.md](references/phrases.md).

## 내 병목 (실패모드의 이름)

입력된 AI 글은 문장이 **완결·균형·통찰의 수행으로 조립되어** 있다. 삼항 병렬,
"A가 아니라 B다", 문단 끝 경구, 선제적 방어 — 전부 "이 단위를 꽉 채워 착지시키자"는
한 충동의 산물이다. 변환할 때 원문을 매끄럽게만 다듬으면 나는 같은 충동으로 다시
조립하게 된다. 사람 작가는 덜 채우고, 비대칭으로 두고, 무게를 한쪽으로 쏠리게 둔다.
다듬고 싶은 충동이 올라오는 순간이 곧 조립으로 돌아가는 순간이다. 멈추고 목소리로.

## 목소리 앵커 (규칙보다 이게 강하다)

시작 전, 대상 글에서 "이건 사람이 썼다" 싶은 문단 1~2개를 고른다. 그 문단을 옆에
두고, 쓰는 문단이 그 결에 붙는지 귀로 듣는다. 사람다운 문단이 공통으로 하는 것:

1. **구체가 은유를 번다.** 추상 은유(지층·구조·레이어)를 쓰기 전에 만질 수 있는
   장면·숫자·사물을 먼저 깐다. 장면 없이 튀어나온 은유는 삭제 후보.
2. **대조가 아니라 이미지로 민다.** "A가 아니라 B다"로 논증하지 말고 그림을 겹쳐라.
3. **착지를 낮춘다.** 마지막 문장을 경구로 터뜨리지 않는다. 조용히 새어나가게 둔다.
4. **감정을 명명하지 않고 배치로 유발한다.** "슬프다" 대신 상황을 놓아 독자가 스스로.

## 금지 문형 (사용자 확정 기준 — "주제라서 남긴다"보다 이게 먼저다)

1. **"A가 아니라 B다" 전면 해체.** B를 직접 서술. 굳이 말하지 않을 사실을 말하지 않는다.
2. **이/그/저/나/내 감축.** 지시어는 드롭 우선, 나/내는 주어 생략으로 재구성.
3. **강조어 금지.** 훨씬·매우·아주 따위.
4. **쉼표는 꼭 필요할 때만.**
5. **긴 문단 분할.** 공백 제외 450자 넘으면 쪼갠다.
6. **의인·물리은유 서술 금지.** "숫자들이 통과한다", "시선의 그물" 류.
7. **메타 선언 금지.** "~덧붙여 둘 사실이 있다", "~적어 둔다", "~할 차례다". 내용으로 바로 시작.
8. **가짜 강조 금지.** "중요한 것은", "핵심은".
9. **선언 연쇄 금지.** 한 생각을 짧은 선언문 여러 개로 쪼개 착지시키지 않는다.
   "나는 리모컨이다. 리모컨은 나다. 그러기 위해 나는 눌린다." → "나는 리모컨인데 눌린다."
   도치 재선언("저자이면서 동시에 독자다")도 같은 병.

주의: "중심 은유 보존" 규칙을 문예체 전체 보호로 확대해석하지 말 것. 보호 대상은
사실·숫자·각주·인용이지, 문형이 아니다. 결과 보고에는 **문단별 쉼표 수**를 반드시
게이트 출력 그대로 싣는다(작업 검증용).

## 손대지 않을 것 (protected spans — 먼저 긋고 시작)

사실·숫자·연도·이름·인용·각주 마커·기술 용어는 그대로 둔다. **그리고 그 글의
주제어는 틱이 아니다.** 예: 어떤 글이 "~하지 않겠다"를 여러 번 반복한다면, 그건 부정
회피 틱이 아니라 그 글의 약속·주제일 수 있다. 이런 건 세지도 고치지도 않는다. (이걸 안
그으면 주제를 틱으로 오탐한다.)

## 실행 절차 (매 파일 반드시 — 한 단계도 건너뛰지 않는다)

이 일곱 단계를 다 거치기 전에는 그 파일을 "변환 완료"라고 말하지 않는다.
이 절차를 지킨 파일은 제대로 나왔고, 5~7을 건너뛰고 인상으로 "완료"라 한 파일은 얕게
나왔다. **검증(5·6·7)은 선택이 아니라 완료의 조건이다.**

1. **읽기** — 원문 전문을 처음부터 끝까지 읽는다. 발췌·요약으로 대신하지 않는다.
2. **protected span 긋기 + 각주 개수 기록** — 각주 마커(`[^x-y]`) 개수, 숫자·연도·
   이름·용어·그 글의 주제어를 목록으로. **각주 개수를 숫자로 적어 둔다**(변환 후 대조용).
3. **앵커 선정** — 원문에서 가장 사람다운 문단 1~2개를 고른다.
4. **1패스 변환 (기본 힘 = structural) — 문단 원장을 남기며 간다.** 파일을 통째로 훑고
   "됐다"는 인상으로 넘어가는 것이 이 스킬이 반복해서 실패한 지점이다. 그래서 작업의
   단위는 파일이 아니라 **문단**이다. 원문 문단을 **처음부터 끝까지 순서대로**, 한 문단도
   건너뛰지 않고 다음 원장을 적으며 지난다:
   > `¶N | 이 문단이 하는 일 | 발견한 틱(삼항·경구착지·정정골격·대시·지시어·긴문단/없음) | 한 것(다시 씀·분할·착지 낮춤) 또는 남긴 이유(주제 척추 등)`

   각 문단은 목소리로 **다시 쓴다**(문장내 치환 아님). 원문의 내용·순서·사실은 지키되,
   조립하지 말고 소리 내어 말하듯. 짧은 문단·착지 없는 문단·밋밋한 문단을 섞는다.
   경구로 끝나는 문단은 착지를 낮추고, 삼항 병렬은 깬다. 추상 자리에 원문이 준 구체를
   앞세운다. **minimal로 시작하지 않는다. 그리고 "다 봤다"는 이 원장이 문단 수만큼
   채워졌다는 뜻이지, 통독 인상이 아니다.**
5. **결정적 게이트 실행 (`mary_gate.py` — 이 수치가 SSOT다).** 변환본에 대해 Bash 1회:
   ```
   python3 ${CLAUDE_SKILL_DIR}/scripts/mary_gate.py --before 원본.md --after 변환본.md
   ```
   - **exit 2 = 각주 유실 등 하드 위반 → 변환본 채택 금지.** 복구 후 재실행. (각주 마커
     하나를 빠뜨리는 사고를 이 게이트가 잡는다. 코드가 완벽히 판정하는 유일한 축.)
   - **exit 1 = 고칠 목록 있음.** v2 게이트는 걸린 자리를 문단 번호+발췌로 출력한다.
     **발췌를 전수 처치**(고치거나, 인용·내용어라 못 고치면 문단 원장에 잔존 사유를
     적기)한 뒤 재실행. 목록이 처치되기 전에는 완료 선언 금지.
   - 출력의 문단 지도(글자수·쉼표)·지시어 지도는 **SSOT 측정값이다. 지어내거나 인상으로
     덮어쓰지 않는다** — 결과 보고엔 이 출력값을 그대로 쓴다.
   - 게이트가 못 보는 것은 한 글자 항목(축·결·위)뿐이다 — 그것만 눈으로.
     오탐 주의: `축`=대개 압축, `결`=결국/연결. 그 글의 주제어는 `--protect`로 제외.
6. **루브릭 채점 (숫자 보고).** 아래 5차원을 **실제로 매겨 숫자로 보고한다.** 합 35/50
   미만이거나 눈에 띄게 낮은 문단이 있으면 그 문단을 다시 쓰고 4~6을 반복한다.
7. **완료 선언 — 조건 미달이면 금지.** 다음이 전부 충족되기 전에는 "완료/다 됐다"고
   말하지 않는다: (a) 4단계 문단 원장이 **원문 문단 수만큼** 채워졌다, (b) `mary_gate.py`가
   **exit 2가 아니다**(각주 보존), (c) 게이트 SSOT 숫자와 루브릭 점수(35/50 이상)를
   숫자로 보였다. 하나라도 없으면 "아직 안 됐다"가 정답이다. 인상으로 "된 것 같다"고
   말하지 않는다.

## 게이트 = 자기채점 루브릭 (grep 아님)

각 문단·글을 5차원 1~10으로 매긴다. 문자열 개수가 아니라 **읽어서 판단한다**
(그래야 주제어를 틱으로 오탐하지 않는다).

| 차원 | 질문 |
|---|---|
| 직접성 | 단언인가, 대조·정정으로 에두르나 |
| 리듬 | 불규칙한가, 삼항·경구로 규칙적인가 |
| 구체 | 장면·숫자가 은유를 버는가, 은유가 공짜로 나오나 |
| 착지 | 문단이 조용히 끝나나, 매번 경구로 터지나 |
| 진정성 | 소리 내어 읽어 사람이 말하는가 |

합 35/50 미만이면 그 문단 재작성.

## 힘 다이얼 (미달이면 세게)

- **minimal** — 문장 안에서만 손봄. 이미 좋은 글에.
- **standard** — 문장 재배열·정정 골격 해체.
- **structural** — 문단 삭제·병합·재배열까지. **루브릭 미달이면 여기까지 올린다.**
  (소심하게 minimal로 돌리면 틱이 안 빠진다. 미달의 대부분은 힘이 약해서다.)

## before → after 예시

**before**: 성장이 아니라 생존이 목표였다고 그는 회고했다. 버티는 자가 이기는 법이라고
그해의 기록은 맺어졌다.

**after**: 그는 그해를 회고하며, 목표는 성장이 아니라 생존이었다고 적었다. 버티다
보면 남는다는 말도 덧붙였다. 기록은 거기서 끝났다.

한 일: ①인용 도치("~다고 …맺어졌다")를 정순으로 ②경구 착지("버티는 자가 이기는 법")
제거 ③짧은 문장으로 리듬을 끊되 삼항으로 각 맞추지 않음 ④사실로 조용히 끝냄.
